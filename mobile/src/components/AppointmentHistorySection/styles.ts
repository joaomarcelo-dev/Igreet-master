import { StyleSheet } from "react-native";

const ContainerStyle = StyleSheet.create({
  contente: {
    flex: 1,
    paddingHorizontal: 10
  },

  containerDate: {

  },

  textDate: {
    textAlign: 'right',
    marginVertical: 8,
    fontSize: 14,
    fontWeight: 'bold',
  }
})

export {
  ContainerStyle,
}